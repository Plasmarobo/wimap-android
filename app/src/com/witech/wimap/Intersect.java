package com.witech.wimap;

public class Intersect {
	private double x;
	private double y;
	private double z;
	
	Intersect(RadialDistance A, RadialDistance B, RadialDistance C)
	{
		double d = Math.pow(A.GetX()-B.GetX(), 2);
		PBC
		PAC
	}

}
